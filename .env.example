# Supabase (optional — uses local file store if not set)
NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Email (Resend) — logs to console if not set
RESEND_API_KEY=
EMAIL_FROM="Pho Ta <bookings@photarestaurants.com>"

# SMS (Twilio) — logs to console if not set
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

# Admin dashboard password
ADMIN_PASSWORD=change-me-in-production

# Cron job auth for reminder endpoint (set in Vercel cron)
CRON_SECRET=
